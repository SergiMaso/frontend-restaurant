import { describe, it, expect } from 'vitest';

// DeleteReservationDialog: confirmation dialog before deleting a reservation.
describe('DeleteReservationDialog', () => {
  it('is defined as a module', async () => {
    expect(true).toBe(true);
  });
});
